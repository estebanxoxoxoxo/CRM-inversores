/** Input: the source code of the investor type, read from the file itself so the prompt can never drift from it. */
import typeSource from "../../types/investor.ts?raw";

export const TYPE_SOURCE = typeSource.trim();
